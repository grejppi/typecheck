.Card {
    @apply select-none transition-shadow duration-500;
}

.Card:hover:not(.Empty) {
    @apply shadow-xl;
}

.CardLabel {
    @apply transition-opacity duration-500;
}

.Card .CardLabel {
    @apply opacity-25;
}

.Card:hover .CardLabel {
    @apply opacity-100;
}
